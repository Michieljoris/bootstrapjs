define (
{
  factory: "I never get to be produced, because this file is loaded with a .js extension"
}

);


describe("In runtest.js", function() {
      	   it("loading js files with .js extension runs them", function() {
      		expect(1).toEqual(1);
      	      });
	 });
